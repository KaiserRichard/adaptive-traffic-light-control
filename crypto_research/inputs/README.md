# ATLC Crypto Research Inputs

This folder stores independent test data for the cryptographic security demonstration.

The cryptography assignment requires independent test data, so this module uses a small ATLC runtime sample file instead of shared public demo data.

Input file:

    atlc_runtime_sample.txt

This file simulates sensitive ATLC runtime information such as signal timing, phase state, vehicle counts, and processing FPS.
