# Raspberry Pi deployment requirements
#
# Install system packages first:
# sudo apt install -y python3-numpy python3-opencv python3-pil python3-yaml python3-psutil
#
# Install CPU-only PyTorch separately:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# Then install this file:
# pip install -r requirements-raspi.txt --no-deps

python-dotenv==1.0.1
requests==2.32.5
pyserial==3.5
ultralytics==8.4.22