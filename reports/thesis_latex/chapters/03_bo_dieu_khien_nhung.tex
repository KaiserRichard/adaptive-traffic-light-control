\section{Bộ điều khiển nhúng và giao tiếp UART}

\subsection{Vai trò của ESP32}

ESP32 là bộ điều khiển thực thi của hệ thống. Host có thể thay đổi model, ngưỡng nhận dạng hoặc nguồn video, nhưng đèn thật không được phụ thuộc trực tiếp vào tốc độ xử lý ảnh. Vì vậy firmware nhận bản tin kế hoạch mức cao, kiểm tra hợp lệ, lưu kế hoạch chờ và chạy FSM cục bộ để điều khiển LED. Đây là phần thể hiện rõ nhất nội dung Vi xử lý trong đề tài: cấu hình chân, task, queue, timer, mutex, trạng thái, watchdog và giao thức serial.

\subsection{Cấu hình phần cứng}

Bảng \ref{tab:gpio-map} trình bày ánh xạ GPIO trong firmware. Hai hướng A và B được mô phỏng bằng sáu LED đỏ-vàng-xanh. Việc ánh xạ bằng hằng số trong \texttt{app\_config.h} giúp thay chân phần cứng mà không làm thay đổi logic FSM.

\begin{table}[H]
\centering
\caption{Ánh xạ GPIO điều khiển đèn.}
\label{tab:gpio-map}
\begin{tabular}{>{\bfseries}lcc}
\toprule
Tín hiệu & Chân ESP32 & Vai trò \\
\midrule
A đỏ & 25 & Dừng hướng A \\
A vàng & 26 & Chuyển pha hướng A \\
A xanh & 27 & Cho phép hướng A đi \\
B đỏ & 14 & Dừng hướng B \\
B vàng & 12 & Chuyển pha hướng B \\
B xanh & 13 & Cho phép hướng B đi \\
\bottomrule
\end{tabular}
\end{table}

Giá trị mặc định của kế hoạch an toàn là \(G_A=20\) s, \(G_B=20\) s, vàng 3 s và all-red 1 s. Miền kiểm tra trong firmware giới hạn xanh 10--45 s, vàng cố định 3 s và all-red cố định 1 s. Các giới hạn này làm cho MCU không nhận kế hoạch quá ngắn hoặc quá dài dù host gửi sai.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  controller/.style={
    draw,
    rounded corners=4pt,
    minimum width=3.2cm,
    minimum height=3.7cm,
    align=center,
    fill=blue!6,
    draw=blue!55!black,
    font=\small
  },
  lane/.style={
    draw,
    rounded corners=3pt,
    fill=gray!4,
    minimum width=3.45cm,
    minimum height=0.62cm,
    text width=3.1cm,
    align=left,
    inner sep=4pt,
    font=\footnotesize
  },
  led/.style={
    circle,
    draw,
    minimum size=0.30cm,
    inner sep=0pt
  },
  wire/.style={-{Latex[length=2mm]}, thick, draw=gray!70!black},
  group/.style={font=\bfseries\small}
]
  \node[controller] (esp) at (0,0) {\textbf{ESP32}\\[0.25em]FreeRTOS controller\\[0.25em]\texttt{gpio\_driver}};

  \node[group] at (-4.4,2.05) {Hướng A};
  \node[lane] (ar) at (-4.35,1.20) {\hspace{0.42cm}\textbf{A đỏ}\hfill GPIO 25};
  \node[lane] (ay) at (-4.35,0.25) {\hspace{0.42cm}\textbf{A vàng}\hfill GPIO 26};
  \node[lane] (ag) at (-4.35,-0.70) {\hspace{0.42cm}\textbf{A xanh}\hfill GPIO 27};
  \node[led, fill=red!70] at (-5.78,1.20) {};
  \node[led, fill=yellow!75] at (-5.78,0.25) {};
  \node[led, fill=green!65] at (-5.78,-0.70) {};

  \node[group] at (4.4,2.05) {Hướng B};
  \node[lane] (br) at (4.35,1.20) {\hspace{0.42cm}\textbf{B đỏ}\hfill GPIO 14};
  \node[lane] (by) at (4.35,0.25) {\hspace{0.42cm}\textbf{B vàng}\hfill GPIO 12};
  \node[lane] (bg) at (4.35,-0.70) {\hspace{0.42cm}\textbf{B xanh}\hfill GPIO 13};
  \node[led, fill=red!70] at (2.92,1.20) {};
  \node[led, fill=yellow!75] at (2.92,0.25) {};
  \node[led, fill=green!65] at (2.92,-0.70) {};

  \draw[wire] (esp.west) ++(0,0.92) -- (ar.east);
  \draw[wire] (esp.west) ++(0,0.00) -- (ay.east);
  \draw[wire] (esp.west) ++(0,-0.92) -- (ag.east);
  \draw[wire] (esp.east) ++(0,0.92) -- (br.west);
  \draw[wire] (esp.east) ++(0,0.00) -- (by.west);
  \draw[wire] (esp.east) ++(0,-0.92) -- (bg.west);

  \node[align=center, font=\footnotesize, text width=10.8cm] at (0,-2.55) {
    FSM chỉ chọn một tổ hợp GPIO hợp lệ tại mỗi trạng thái; thay đổi chân nằm trong \texttt{app\_config.h}, không nằm trong logic chuyển pha.
  };
\end{tikzpicture}
\caption{Sơ đồ ánh xạ ESP32 GPIO tới sáu LED mô phỏng hai hướng giao thông.}
\label{fig:gpio-mapping}
\end{figure}

\subsection{Giao thức UART mức ứng dụng}

Bản tin điều khiển dùng định dạng văn bản một dòng:

\begin{lstlisting}
PLAN,<plan_id>,<green_a>,<green_b>,<yellow>,<all_red>
\end{lstlisting}

Ví dụ:

\begin{lstlisting}
PLAN,17,25,15,3,1
\end{lstlisting}

Nếu hợp lệ, MCU phản hồi:

\begin{lstlisting}
ACK,17
\end{lstlisting}

Nếu sai định dạng hoặc ngoài miền giá trị, MCU phản hồi NACK kèm lý do:

\begin{lstlisting}
NACK,18,GREEN_A_OUT_OF_RANGE
\end{lstlisting}

UART phù hợp với nguyên mẫu vì đơn giản, có sẵn trên ESP32, dễ debug bằng Serial Monitor và đủ băng thông cho bản tin thời gian nhỏ. Trong code host, PySerial đóng vai trò mở cổng, gửi dòng PLAN, đọc ACK/NACK và đo độ trễ phản hồi \cite{pyserial_api,pena_uart}. Trong firmware, chỉ parser mới hiểu cú pháp PLAN; các task khác nhận dữ liệu đã được đóng gói thành struct.

\subsection{Kiến trúc FreeRTOS}

Firmware được tổ chức theo kiểu nhiều task nhỏ, mỗi task sở hữu một trách nhiệm rõ ràng. Queue dùng để truyền dữ liệu có nội dung; task notification dùng để đánh thức task khi có sự kiện; timer dùng để phát nhịp báo cáo; mutex dùng để bảo vệ Serial output. Cách dùng này phù hợp với mô hình FreeRTOS về queue, timer và direct-to-task notification \cite{barry_freertos,freertos_queues,freertos_timers,freertos_task_notifications}.

\placeholderfigure{figures/freertos_task_queue_architecture.png}{Cấu trúc FreeRTOS trên ESP32: callback UART, task nhận dữ liệu, task parser, queue kế hoạch, FSM, STATUS reporter, timer và mutex Serial.}{fig:placeholder-freertos}

\begin{table}[H]
\centering
\caption{Task và đối tượng FreeRTOS chính.}
\label{tab:freertos-tasks}
\small
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.18}
\begin{tabularx}{\linewidth}{R{3.1cm}Y}
\toprule
\textbf{Thành phần} & \textbf{Cấu hình và trách nhiệm} \\
\midrule
UART RX task & Stack 4096, ưu tiên 3. Task nhận notification từ UART callback, đọc byte từ Serial, ghép dòng và gửi \texttt{RawMessage} vào queue thô. \\
Parser task & Stack 4096, ưu tiên 2. Task nhận \texttt{RawMessage}, kiểm tra \texttt{PLAN}, validate miền giá trị và gửi \texttt{SignalPlan} vào queue kế hoạch. \\
FSM task & Stack 4096, ưu tiên 2. Task chạy FSM đèn, kiểm tra queue không chặn, áp dụng kế hoạch tại biên an toàn và xử lý host timeout. \\
STATUS task & Stack 3072, ưu tiên 1. Task ngủ chờ notification từ timer, lấy snapshot trạng thái và in một dòng \texttt{STATUS}. \\
Status timer & Software timer chu kỳ 1 s. Timer chỉ đánh thức STATUS task, không tự format hoặc in Serial trong callback. \\
DIAG timer & Software timer chu kỳ 5 s. Timer phục vụ chẩn đoán heap và stack high-water mark trong quá trình chạy thử. \\
Serial mutex & Mutex bảo vệ Serial output để \texttt{ACK}, \texttt{NACK}, \texttt{STATUS}, \texttt{DIAG} và log debug không bị trộn dòng. \\
\bottomrule
\end{tabularx}
\normalsize
\end{table}

\subsection{Đường đi của bản tin PLAN trong firmware}

Luồng nhận UART được thiết kế theo nguyên tắc deferred processing. Callback UART không parse lệnh, không cập nhật FSM và không ghi log dài. Callback chỉ đánh thức \texttt{TaskUARTReceive}. Task này mới đọc dữ liệu serial, tạo \texttt{RawMessage} và chuyển cho parser. Nhờ vậy ngữ cảnh callback rất ngắn, tránh đưa logic ứng dụng vào nơi khó kiểm soát thời gian.

\placeholderfigure{figures/uart_rx_deferred_processing.png}{Luồng xử lý UART kiểu deferred processing: callback chỉ notify, task nhận UART mới đọc byte và đưa dữ liệu vào queue.}{fig:placeholder-uart-deferred}

\begin{table}[H]
\centering
\caption{Các bước xử lý bản tin PLAN trong firmware.}
\label{tab:plan-path}
\footnotesize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.25}
\begin{tabularx}{\linewidth}{C{0.8cm}R{4.3cm}Y}
\toprule
\textbf{Bước} & \textbf{Thành phần} & \textbf{Xử lý} \\
\midrule
1 & UART RX callback & Được gọi khi dòng serial idle; gửi task notification cho \texttt{TaskUARTReceive}. \\
2 & \texttt{TaskUARTReceive} & Đọc \texttt{Serial.available()}, gom byte đến ký tự xuống dòng, tạo \texttt{RawMessage}. \\
3 & \texttt{rawMessageQueue} & Lưu bản tin thô theo copy, tách đọc serial khỏi parse protocol. \\
4 & \texttt{TaskPlanParser} & Kiểm tra prefix \texttt{PLAN,}, dùng \texttt{sscanf} parse 5 trường số. \\
5 & \texttt{validateSignalPlan} & Kiểm tra \texttt{plan\_id}, xanh, vàng và all-red trong miền cấu hình. \\
6 & \texttt{planQueue} & Chỉ nhận \texttt{SignalPlan} đã hợp lệ; parser trả ACK khi gửi queue thành công. \\
7 & \texttt{TaskTrafficFSM} & Nhận kế hoạch không chặn, lưu thành \texttt{pendingPlan}, không đổi active plan ngay giữa chu kỳ. \\
\bottomrule
\end{tabularx}
\normalsize
\end{table}

\subsection{Máy trạng thái đèn giao thông}

Logic đèn giao thông không nên được viết như một nhóm lệnh bật tắt rời rạc, vì cách đó dễ tạo ra tổ hợp đèn không an toàn khi mở rộng thêm pha hoặc thêm điều kiện lỗi. Trong firmware, chu kỳ đèn được mô hình hóa bằng FSM cục bộ gồm sáu trạng thái hợp lệ. Transition chỉ xảy ra khi thời lượng của trạng thái hiện tại đã hết; kế hoạch mới chỉ ảnh hưởng tới thời lượng của chu kỳ sau, không làm thay đổi thứ tự pha.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  state/.style={
    draw,
    rounded corners=3pt,
    align=center,
    minimum width=3.25cm,
    minimum height=1.05cm,
    font=\small
  },
  arrow/.style={-{Latex[length=2.5mm]}, thick},
  every node/.style={transform shape}
]
  \node[state, fill=green!12]  (ag)  at (0,0)   {\texttt{A\_GREEN}\\A xanh, B đỏ};
  \node[state, fill=yellow!18] (ay)  at (4.15,0) {\texttt{A\_YELLOW}\\A vàng, B đỏ};
  \node[state, fill=red!10]    (ara) at (8.3,0)  {\texttt{ALL\_RED\_AFTER\_A}\\A đỏ, B đỏ};
  \node[state, fill=green!12]  (bg)  at (8.3,-2.15) {\texttt{B\_GREEN}\\A đỏ, B xanh};
  \node[state, fill=yellow!18] (by)  at (4.15,-2.15) {\texttt{B\_YELLOW}\\A đỏ, B vàng};
  \node[state, fill=red!10]    (arb) at (0,-2.15) {\texttt{ALL\_RED\_AFTER\_B}\\A đỏ, B đỏ};

  \draw[arrow] (ag) -- node[above, font=\scriptsize] {\(G_A\) hết} (ay);
  \draw[arrow] (ay) -- node[above, font=\scriptsize] {vàng} (ara);
  \draw[arrow] (ara) -- node[right, font=\scriptsize] {all-red} (bg);
  \draw[arrow] (bg) -- node[below, font=\scriptsize] {\(G_B\) hết} (by);
  \draw[arrow] (by) -- node[below, font=\scriptsize] {vàng} (arb);
  \draw[arrow] (arb) -- node[left, font=\scriptsize] {all-red} (ag);
\end{tikzpicture}
\caption{FSM cục bộ trên MCU chỉ cho phép các pha đèn hợp lệ và chuyển pha theo timing plan đã được kiểm tra.}
\label{fig:mcu-fsm}
\end{figure}

\begin{table}[H]
\centering
\caption{Đầu ra GPIO theo trạng thái FSM.}
\label{tab:fsm-output}
\small
\begin{tabularx}{\linewidth}{>{\bfseries}p{3.4cm}YY}
\toprule
Trạng thái & Hướng A & Hướng B \\
\midrule
\texttt{A\_GREEN} & Xanh bật & Đỏ bật \\
\texttt{A\_YELLOW} & Vàng bật & Đỏ bật \\
\texttt{ALL\_RED\_AFTER\_A} & Đỏ bật & Đỏ bật \\
\texttt{B\_GREEN} & Đỏ bật & Xanh bật \\
\texttt{B\_YELLOW} & Đỏ bật & Vàng bật \\
\texttt{ALL\_RED\_AFTER\_B} & Đỏ bật & Đỏ bật \\
\bottomrule
\end{tabularx}
\normalsize
\end{table}

\texttt{TaskTrafficFSM} không chờ \texttt{planQueue} bằng \texttt{portMAX\_DELAY}. Task chỉ kiểm tra queue với timeout bằng 0, rồi tiếp tục cập nhật thời gian trạng thái. Đây là chi tiết quan trọng: nếu không có kế hoạch mới, FSM vẫn chạy. Queue chỉ bổ sung kế hoạch khi có dữ liệu hợp lệ, không được quyền làm dừng điều khiển đèn.

\subsection{Áp dụng kế hoạch tại biên an toàn}

ACK không có nghĩa là đèn đổi ngay. ACK chỉ xác nhận bản tin đã được parse, validate và đưa vào queue kế hoạch. Khi FSM nhận kế hoạch, nó lưu vào \texttt{pendingPlan}. Kế hoạch này chỉ trở thành \texttt{activePlan} khi FSM quay về trạng thái \texttt{A\_GREEN}, tức bắt đầu một chu kỳ mới. Cơ chế này tránh trường hợp đang giữa pha xanh mà thời lượng bị thay đổi đột ngột.

\placeholderfigure{figures/safe_plan_apply_timeline.png}{Timeline áp dụng kế hoạch an toàn: PLAN mới được lưu thành pending plan và chỉ chuyển thành active plan ở đầu chu kỳ.}{fig:placeholder-safe-apply}

\subsection{Watchdog và trạng thái dự phòng}

MCU lưu thời điểm nhận kế hoạch hợp lệ gần nhất. Nếu khoảng im lặng của host vượt ngưỡng 30 giây, FSM đặt \texttt{health=HOST\_TIMEOUT}, bỏ kế hoạch chờ cũ và chuyển sang kế hoạch mặc định. Đây là lựa chọn đúng cho điều khiển nhúng: dữ liệu AI quá cũ không nên tiếp tục được coi là tình trạng giao thông hiện tại.

\begin{table}[H]
\centering
\caption{Chính sách an toàn khi host lỗi hoặc bản tin sai.}
\label{tab:safety-policy}
\small
\begin{tabularx}{\linewidth}{>{\bfseries}p{3.8cm}YY}
\toprule
Tình huống & Phản ứng firmware & Lý do \\
\midrule
Sai prefix lệnh & NACK \texttt{UNKNOWN\_COMMAND} & Không để lệnh lạ đi vào parser kế hoạch. \\
Sai số trường & NACK \texttt{MALFORMED\_PLAN} & Tránh đọc thiếu hoặc lệch trường timing. \\
Xanh ngoài miền & NACK tương ứng & Giữ thời gian xanh trong khoảng 10--45 s. \\
Vàng/all-red sai miền & NACK tương ứng & Giữ khoảng chuyển pha cố định trong nguyên mẫu. \\
Host timeout & Dùng default plan & Không tiếp tục tin vào dữ liệu đã cũ. \\
Serial đang bận & Log telemetry có thể bỏ qua & Không để in log làm kẹt điều khiển. \\
\bottomrule
\end{tabularx}
\normalsize
\end{table}

\subsection{STATUS, DIAG và mutex Serial}

STATUS là bản tin máy đọc được:

\begin{lstlisting}
STATUS,<plan_id>,<state>,<remaining_seconds>,<health>
\end{lstlisting}

Ví dụ:

\begin{lstlisting}
STATUS,17,A_GREEN,12,OK
\end{lstlisting}

Trong firmware, timer không trực tiếp in STATUS. Timer chỉ gửi notification tới \texttt{TaskStatusReporter}. Task này lấy snapshot trạng thái đã được FSM cập nhật, format dòng STATUS rồi in qua mutex Serial. Snapshot được bảo vệ bằng critical section ngắn để tránh đọc nửa cũ nửa mới trên ESP32 hai nhân. Thiết kế này tách rõ điều khiển và giám sát: FSM sở hữu trạng thái thật, reporter chỉ công bố trạng thái.

\placeholderfigure{figures/status_notification_flow.png}{Luồng STATUS bằng task notification: software timer chỉ đánh thức task reporter, còn task reporter mới format và in STATUS.}{fig:placeholder-status}

Serial mutex giải quyết một lỗi thực tế trong firmware đa task: nhiều task cùng in Serial có thể làm ACK, NACK, STATUS và DIAG xen vào nhau. Firmware xây dựng cả dòng trước, sau đó khóa mutex và in nguyên dòng. ACK/NACK dùng bounded wait vì là protocol-critical; telemetry như STATUS/DIAG có thể dùng best-effort để không cản trở FSM.
