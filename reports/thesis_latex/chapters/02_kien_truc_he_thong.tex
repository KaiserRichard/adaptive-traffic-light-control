\section{Kiến trúc hệ thống với trọng tâm nhúng}

\subsection{Tổng quan kiến trúc}

Kiến trúc \projectname{} được chia thành hai tầng. Tầng host xử lý video, phát hiện phương tiện, ước lượng mật độ và tính kế hoạch thời gian. Tầng MCU nhận kế hoạch đã chuẩn hóa, kiểm tra miền giá trị và thực thi đèn giao thông bằng máy trạng thái hữu hạn. Cách chia này cố ý đặt phần điều khiển thời gian thực ở ESP32, còn AI chỉ đóng vai trò cảm nhận và đề xuất.

\begin{figure}[H]
  \centering
  \safeincludegraphics[width=0.88\linewidth]{figures/atlc_block_diagram.png}
  \caption{Sơ đồ khối tổng thể ATLC, thể hiện camera/video, pipeline AI, bộ lập kế hoạch, khối giám sát và bộ điều khiển nhúng.}
  \label{fig:atlc-block}
\end{figure}

Điểm quan trọng của kiến trúc là host không bật tắt GPIO trực tiếp theo từng frame. Host chỉ gửi một kế hoạch thời gian mức cao. ESP32 mới là thành phần thực thi tín hiệu đèn. Nhờ vậy, khi YOLO suy luận chậm, giao diện hiển thị bị lag hoặc camera mất khung hình tạm thời, chu kỳ đèn trên MCU vẫn tiếp tục chạy theo kế hoạch đang hoạt động.

\begin{figure}[H]
  \centering
  \safeincludegraphics[width=0.86\linewidth]{figures/host_mcu_split.png}
  \caption{Phân tầng host/Raspberry Pi xử lý thị giác máy tính và ESP32/MCU thực thi FSM điều khiển đèn.}
  \label{fig:host-mcu}
\end{figure}

\subsection{Cấu hình khối chức năng kỹ thuật}

Sơ đồ kiến trúc chỉ cho thấy quan hệ giữa các khối, nhưng chưa đủ để kiểm tra hệ thống có triển khai được hay không. Vì vậy, cấu hình kỹ thuật cần được diễn giải theo đầu vào, đầu ra và tham số vận hành của từng khối. Bảng \ref{tab:block-config} đóng vai trò như bản đặc tả rút gọn: nó liên kết ý tưởng kiến trúc với các điểm cấu hình thật trong runtime.

\small
\begin{longtable}{B{1.3cm}L{3.0cm}L{4.1cm}L{4.2cm}}
\caption{Cấu hình khối chức năng kỹ thuật.}
\label{tab:block-config}\\
\toprule
Mã & Khối & Chức năng kỹ thuật & Tham số và đầu ra chính \\
\midrule
\endfirsthead
\toprule
Mã & Khối & Chức năng kỹ thuật & Tham số và đầu ra chính \\
\midrule
\endhead
TR1 & Thu nhận hình ảnh & Đọc video/camera bằng OpenCV, kiểm tra kích thước frame và cảnh báo khi ROI không khớp độ phân giải. & \texttt{VIDEO\_SOURCE}, \texttt{CAMERA\_WIDTH}, \texttt{CAMERA\_HEIGHT}, frame BGR. \\
TR2 & Detector YOLO & Chạy model cục bộ qua Ultralytics, trả về bounding box, confidence và class name đã chuẩn hóa. & \texttt{YOLO\_MODEL\_PATH}, \texttt{YOLO\_IMGSZ}, \texttt{CONFIDENCE\_THRESHOLD}. \\
TR3 & ROI/counting & Gán detection vào hướng A/B bằng tâm bounding box và polygon ROI; đếm số xe theo lớp. & \texttt{roi\_camera.json} hoặc \texttt{roi\_example.json}; output: \texttt{counts\_a}, \texttt{counts\_b}. \\
TR4 & Density & Tính mật độ thô, PCE và EMA để giảm dao động từng frame. & Trọng số PCE: motorbike 0.5, car 1.0, bus/truck 2.5; \(\alpha=0.30\). \\
TR5 & Scheduler & Chia ngân sách đèn xanh theo tỷ lệ mật độ, sau đó clamp vào miền an toàn. & \(G_{base}=20\), \(G_{min}=10\), \(G_{max}=45\), vàng 3 s, all-red 1 s. \\
TR6 & Runtime controller & Tách \texttt{raw\_signal\_plan}, \texttt{pending\_plan} và \texttt{active\_plan}; áp dụng kế hoạch tại đầu chu kỳ. & Output ổn định cho overlay và UART; tránh đổi đèn theo từng frame. \\
TR7 & UART sender & Serialize kế hoạch thành dòng PLAN, gửi qua serial và chờ ACK/NACK. & Baud 115200; timeout ACK 2 s; gửi khi kế hoạch thay đổi đủ ngưỡng hoặc sang chu kỳ mới. \\
TR8 & ESP32 parser & Nhận dòng PLAN từ queue thô, parse trường dữ liệu, validate miền giá trị và đẩy kế hoạch hợp lệ sang queue kế hoạch. & ACK khi hợp lệ; NACK với lý do khi sai định dạng hoặc ngoài miền. \\
TR9 & ESP32 FSM & Chạy sáu trạng thái đèn, điều khiển GPIO và áp dụng kế hoạch mới tại biên an toàn. & A/B đỏ-vàng-xanh; tick 50 ms; default plan 20/20/3/1. \\
TR10 & Giám sát & Phát STATUS/DIAG, ghi runtime log và tạo biểu đồ evidence cho báo cáo. & STATUS mỗi 1 s; DIAG mỗi 5 s; log CSV/JSONL phía host. \\
\bottomrule
\end{longtable}
\normalsize

\subsection{Luồng dữ liệu}

Luồng dữ liệu được tổ chức thành các bước tuần tự:

\begin{enumerate}
  \item Camera hoặc video cung cấp frame đầu vào.
  \item Detector tạo danh sách phương tiện đã nhận dạng.
  \item ROI tách phương tiện theo hướng đường.
  \item Density chuyển số lượng xe thành tín hiệu có trọng số PCE và làm mượt.
  \item Scheduler sinh kế hoạch thời gian xanh cho hai hướng.
  \item Runtime controller lưu kế hoạch mới như kế hoạch chờ, chỉ áp dụng tại biên chu kỳ.
  \item UART sender gửi kế hoạch đang hoạt động xuống ESP32.
  \item ESP32 parser kiểm tra bản tin và FSM thực thi đèn.
\end{enumerate}

Điểm kiểm soát chính nằm ở bước 6 và bước 8. Bước 6 ngăn tầng AI làm đèn đổi liên tục theo từng frame. Bước 8 ngăn bản tin sai định dạng hoặc vượt giới hạn timing đi vào FSM.

\subsection{Mapping yêu cầu sang khối triển khai}

Một yêu cầu chức năng chỉ có giá trị kỹ thuật khi có thể chỉ ra module chịu trách nhiệm và bằng chứng triển khai tương ứng. Do đó bảng mapping dưới đây không nhằm liệt kê toàn bộ đường dẫn mã nguồn, mà nhằm chứng minh mỗi yêu cầu chính đã có một điểm hiện thực hóa rõ ràng trong hệ thống.

\small
\begin{longtable}{B{1.4cm}L{4.4cm}L{7.2cm}}
\caption{Liên kết yêu cầu chức năng với module triển khai.}
\label{tab:req-to-module}\\
\toprule
Yêu cầu & Module chính & Bằng chứng triển khai \\
\midrule
\endfirsthead
\toprule
Yêu cầu & Module chính & Bằng chứng triển khai \\
\midrule
\endhead
FR1 & Detector factory và YOLO backend & Backend \texttt{yolo} khởi tạo \texttt{LocalYoloDetector}; output detection được chuẩn hóa trước khi đi vào ROI và density. \\
FR2 & ROI module & ROI đọc từ JSON; hàm kiểm tra biên phát hiện khi tọa độ ROI không khớp độ phân giải frame. \\
FR3 & Counting và density module & Hệ thống có raw count, PCE-weighted density và \texttt{EMASmoother} để giảm dao động frame-to-frame. \\
FR4 & Scheduler module & Hàm lập kế hoạch tính tỷ lệ mật độ, phân bổ thời gian xanh và clamp vào miền timing an toàn. \\
FR5 & Runtime controller & \texttt{active\_plan} được tách khỏi \texttt{pending\_plan}, nên kế hoạch thô không làm đèn đổi ngay giữa chu kỳ. \\
FR6 & UART sender và firmware protocol & PLAN, ACK và NACK dùng định dạng một dòng, đủ đơn giản để host và MCU cùng parse được. \\
FR7 & Traffic FSM task & FSM sáu trạng thái điều khiển GPIO trên ESP32, không phụ thuộc vào việc host gửi lệnh liên tục. \\
FR8 & Status reporter service & STATUS lấy snapshot trạng thái và được phát bằng task notification thay vì in trực tiếp trong timer callback. \\
FR9 & Traffic FSM watchdog & Khi host timeout, FSM chuyển sang fallback plan và phản ánh \texttt{HOST\_TIMEOUT} trong STATUS. \\
FR10 & Runtime logger và benchmark scripts & Log runtime và biểu đồ direction count, green time, FPS cung cấp bằng chứng để kiểm tra lại hành vi pipeline. \\
\bottomrule
\end{longtable}
\normalsize

\subsection{Lý do chọn kiến trúc hai tầng}

Nếu host vừa chạy detector vừa điều khiển GPIO, thời gian đèn sẽ phụ thuộc vào tải suy luận, ghi video, hiển thị cửa sổ và độ ổn định camera. Đây là cách thiết kế yếu đối với bài toán nhúng vì nhiệm vụ điều khiển cần thời gian và trạng thái rõ ràng hơn nhiệm vụ nhận dạng ảnh. Trong kiến trúc hiện tại, host có thể chậm nhưng MCU vẫn chạy chu kỳ cũ; host có thể gửi bản tin sai nhưng parser sẽ NACK; host có thể im lặng nhưng watchdog sẽ chuyển sang kế hoạch dự phòng.

Kiến trúc này cũng giúp kiểm thử thuận lợi. Pipeline host có thể chạy với fake MCU trước khi nối ESP32 thật. Firmware có thể được kiểm tra bằng Serial Monitor với các bản tin PLAN thủ công. Khi thay detector hoặc huấn luyện model mới, phần ROI, scheduler và firmware không cần thay đổi giao diện dữ liệu.
